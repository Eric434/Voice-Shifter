kind = "web"
previewPath = "/"
title = "Voice Masker"
version = "1.0.0"
id = "artifacts/voice-masker"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21351

[services.development]
run = "pnpm --filter @workspace/voice-masker run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/voice-masker", "run", "build" ]
publicDir = "artifacts/voice-masker/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21351"
BASE_PATH = "/"
